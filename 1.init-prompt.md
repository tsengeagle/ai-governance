請根據我提供的 `ai/project-context-bootstrap.yaml`，初始化此 repository 的 GitHub Copilot / agent mode 治理文件。

這是一個 **repository init** 任務。  
你的目標不是只根據 repo 結構產出文件，而是要根據：

1. `ai/project-context-bootstrap.yaml`
2. repository 實際結構
3. bootstrap 指定的 governance repo
4. bootstrap 指定的 governance / workflow / shared baseline

共同收斂出此 repository 的第一版治理結構。

---

## 0. 初始化前的 authority source

在開始前，先讀取並整理：

### A. bootstrap
- `ai/project-context-bootstrap.yaml`

### B. repository 實際結構
- repo 目錄
- 主要模組
- build/test/run/lint 入口
- 代表性檔案
- 技術棧跡象

### C. governance repo 與 baseline rules
若 `ai/project-context-bootstrap.yaml` 中：
- `ai_governance.governance_repo` 存在
- 且有任何一組 baseline rules 被啟用或指定

則你必須先把 bootstrap 指定的 governance repo 視為 **authority source** 之一，並依下列規則載入：

#### C1. governance baseline
若存在：
- `ai_governance.governance_rules.base_path`
- `ai_governance.governance_rules.required`

則你必須依：
- `ai_governance.governance_repo.url`
- `ai_governance.governance_repo.default_ref`
- `ai_governance.governance_rules.base_path`
- `ai_governance.governance_rules.required`

載入 governance baseline。

此 baseline 用來定義：
- workflow invariants
- engineering principles
- evidence policy
- 或其他核心治理規則

#### C2. workflow baseline
若存在：
- `ai_governance.workflow_rules.base_path`
- `ai_governance.workflow_rules.required`

則你必須依：
- `ai_governance.governance_repo.url`
- `ai_governance.governance_repo.default_ref`
- `ai_governance.workflow_rules.base_path`
- `ai_governance.workflow_rules.required`

載入 workflow baseline。

此 baseline 用來定義：
- implement-change
- fix-bug
- review-code
- 或其他標準工作流程文件

#### C3. shared baseline
若：
- `ai_governance.use_shared_rules: true`
- 且存在 `ai_governance.shared_rules.base_path`
- 且存在 `ai_governance.shared_rules.required`

則你必須依：
- `ai_governance.governance_repo.url`
- `ai_governance.governance_repo.default_ref`
- `ai_governance.shared_rules.base_path`
- `ai_governance.shared_rules.required`

載入 shared rules baseline。

此 baseline 用來定義：
- 跨 repository 共用的 shared engineering rules
- architecture / design / compatibility / commit baseline
- 或其他組織級共享規則

### D. baseline 收斂原則
上述 governance / workflow / shared baselines 都屬於初始化治理文件的正式基線之一。  
你不得忽略它們，也不得只根據 bootstrap 欄位名稱與 repo 結構自行發明治理內容。

你必須做的是：
- 讀取 baseline
- 辨識哪些規則屬於 repository-wide
- 哪些適合收斂到 path-specific instructions
- 哪些應進入 project context / operating guide
- 以 repo-specific 方式收斂
- 不可整份原文照搬進專案 repo

### E. 讀取失敗或資訊不足時的處理
若 bootstrap 指定了 governance repo 或任一 baseline，但初始化時無法完整讀取，必須：

- 明確標示為 `assumption` 或 `pending confirmation`
- 在 `ai/project-context.yaml` 中記錄缺失
- 在輸出 summary 中揭露哪些 authority source 未完整讀取
- 不可假裝已完成 baseline 收斂
- 不可將未讀取內容包裝成已確認規則

若缺少 `governance_repo.default_ref`、baseline `base_path`、或 `required` 清單，亦屬於未完整可解析輸入，必須揭露，不可自行補造。

---

## 1. 先判斷主要技術棧

先根據 bootstrap 與 repo 結構判斷主要技術棧。不要預設所有 repo 都是 Grails。需至少能辨識：

- Grails legacy backend
- Flash / AIR / Flex legacy frontend
- Spring Boot backend
- Angular frontend
- Vue.js frontend
- Python / Flask backend

若 repo 為其他技術棧，請根據實際結構判斷並處理。

在輸出文件前，先明確說明：
1. 你判斷出的主要技術棧
2. 判斷依據
3. 哪些部分已確認
4. 哪些部分仍屬 assumption / pending confirmation

---

## 2. 請固定產出以下共通文件

### 1. `.github/copilot-instructions.md`
- repository-wide 全域硬規則
- 需收斂 governance baseline、workflow baseline、shared baseline 與 repo 實際情況
- 包含至少：
  - backward compatibility
  - legacy grandfathering
  - no incidental refactor
  - validation
  - completion format
  - repository-wide 層級的 baseline 收斂結果

### 2. `AGENTS.md`
- repo operating guide
- 放：
  - repository map
  - build/test/run/lint 或其他可用 gate 入口
  - inspection order
  - representative files
  - docs index
  - escalation rules
- 必須 repo-specific
- 不要重複全域硬規則
- 若某些 build/test/run/lint/gate 入口無法確認，需明確標示 assumptions / pending confirmation

### 3. `ai/project-context.yaml`
- authoritative project context
- 由 bootstrap、governance baseline、workflow baseline、shared baseline 與 repo 實況收斂而成
- 若資訊不足，標示 assumptions / pending confirmation，不可虛構
- 應反映：
  - repo 實際技術棧
  - architecture / constraints
  - repository 專屬風險
  - 哪些 baseline 已收斂進本 repo
  - 哪些 baseline 僅部分收斂
  - 尚未完成確認的內容

---

## 3. 再依技術棧產出 path-specific instructions

- Grails:
  - grails-entrypoints.instructions.md
  - grails-service.instructions.md
  - grails-domain.instructions.md

- Spring Boot:
  - springboot-entrypoints.instructions.md
  - springboot-service.instructions.md
  - springboot-domain.instructions.md

- Flex / AIR / Flash:
  - flex-ui.instructions.md
  - flex-service-adapter.instructions.md
  - flex-state-event.instructions.md

- Angular:
  - angular-components.instructions.md
  - angular-services.instructions.md
  - angular-state-routing.instructions.md

- Vue.js:
  - vue-components.instructions.md
  - vue-state-api.instructions.md

- Python / Flask:
  - flask-entrypoints.instructions.md
  - flask-service.instructions.md
  - flask-domain.instructions.md
  - flask-integration-client.instructions.md
  - flask-config-appfactory.instructions.md

- 其他技術棧：
  - 根據 repo 結構與 bootstrap 判斷，產出相應的 path-specific instruction 文件

---

## 4. 所有 path-specific instructions 必須符合以下要求

所有 path-specific instructions 必須：

- 使用 `applyTo`
- 說明角色
- 列 `allowed / forbidden behaviors`
- 有 `legacy grandfathering clause`
- 有 `when in doubt`
- 不可為了遵守規則而強制重構既有程式碼
- 必須收斂 governance / workflow / shared baseline 與該技術棧 / 該層級的 repo-specific 實況
- 不可只是把 baseline 原文直接搬進 path-specific 文件

---

## 5. 設計要求

- 文件角色清楚，不可大量重複
- 全域規則、分層規則、repo context、project context 要分開
- 規則主要套用於新程式碼、實質修改邏輯、與新增邏輯
- 若理想架構與向下相容衝突，優先 backward compatibility
- AGENTS.md 必須 repo-specific
- 不要把 bootstrap 原文直接複製進所有文件；請做收斂、去重、分工
- 不要把 governance repo 的 baseline 原文整份複製進專案 repo；請做 repo-specific 收斂
- 若 bootstrap、baseline、repo 結構三者之間有衝突，請優先：
  1. repository 實際結構與已確認事實
  2. governance / workflow / shared baseline
  3. bootstrap 預設值
- 衝突或資訊不足時，必須標示 assumptions / pending confirmation，不可自行補造
- baseline 的角色是提供治理基線與約束方向，不是作為專案 repo 內文件的原文模板；初始化產物必須以 repo-specific 收斂結果為準

---

## 6. 驗證入口要求

初始化時，必須明確辨識並寫入本 repo 的：
- build 入口
- test 入口
- run 入口
- lint / static analysis / 其他可用 gate 入口

若某項入口不存在或無法確認，必須在 `ai/project-context.yaml` 中標示為：
- `assumption`
- 或 `pending confirmation`

不得略過不提。

---

## 7. 輸出順序

請依以下順序輸出：

### Step 1. Governance Inputs Summary
先輸出：
1. bootstrap 中的關鍵治理輸入
2. governance repo / default ref / baseline groups
3. 哪些 governance rules 已成功讀取
4. 哪些 workflow rules 已成功讀取
5. 哪些 shared rules 已成功讀取
6. 哪些 baseline 或 authority source 仍待確認

### Step 2. Tech Stack Decision
輸出：
1. 判斷出的主要技術棧
2. 判斷依據
3. assumptions / pending confirmation

### Step 3. Suggested File Tree
輸出建議的治理文件樹。

### Step 4. File-by-File Output Plan
先說明：
1. 每份文件的角色
2. 為何放在該位置
3. 與其他文件的邊界
4. governance / workflow / shared baseline 將如何被收斂，而不是原文複製

### Step 5. Output File Contents
最後逐一輸出文件內容。

---

## 8. Completion Rules

你不得宣稱：
- 已完整吸收所有 governance repo 規則
- 已完全確認所有 repo context
- 已完整消除所有 assumptions

除非有足夠 evidence 支持。

若存在以下情況，必須明確揭露：
- governance repo 有指定但未完整讀取
- governance baseline 僅部分收斂
- workflow baseline 僅部分收斂
- shared baseline 僅部分收斂
- build/test/run/lint 入口未完整確認
- 部分 stack 判斷仍為 assumption
- 某些 path-specific files 仍待後續補齊