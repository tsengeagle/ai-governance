請針對這個已完成 init 的 repository，執行一次 assumptions / pending confirmations burn-down。

這不是一般 continue 任務。  
你的任務是：

- 讀取 repo 內既有治理文件
- 讀取 `ai/project-context.yaml` 中記錄的 assumptions / pending confirmations
- 讀取 `ai/project-context-bootstrap.yaml`【若存在】
- 讀取本次任務提供的 baseline instance【若提供】
- 針對每一條 assumption / pending confirmation，嘗試以 evidence 消減
- 不得虛構
- 不得把未確認項目包裝成已驗證事實
- 不得在缺乏 authority source 時宣稱 assumptions 已消除

---

## Authority Sources

僅可使用以下 authority sources：

### A. repo 內既有治理文件
- `.github/copilot-instructions.md`
- `AGENTS.md`
- `ai/project-context.yaml`
- `.github/instructions/*.instructions.md`

### B. bootstrap【若存在】
- `ai/project-context-bootstrap.yaml`

必須特別檢查：
- `ai_governance.governance_repo`
- `ai_governance.governance_rules`
- `ai_governance.workflow_rules`
- `ai_governance.shared_rules`

### C. baseline instance【若本次提供】
若本次提供 baseline instance，則其為本次 burn-down 的正式 baseline authority source。  
必須分開辨識：
- governance baseline
- workflow baseline
- shared baseline

### D. repository 實際結構與可驗證入口
- repo 目錄
- representative files
- build/test/run/lint/static analysis/gate 入口
- 可直接檢查的設定檔與腳本

---

## 任務目標

你必須逐條整理 `ai/project-context.yaml` 中的 assumptions / pending confirmations，判斷每一條目前屬於哪一種狀態：

- confirmed
- still assumption
- pending confirmation
- not verifiable in this run
- not in scope for this run

---

## 特別要求：三層 baseline 驗證

除非 evidence 已足夠，否則不得將以下任一項宣稱為已確認：

### governance baseline
- 已成功定位來源
- 已成功讀取 required rules
- 已成功收斂到 repo 文件

### workflow baseline
- 已成功定位來源
- 已成功讀取 required rules
- 已成功收斂到 repo 文件

### shared baseline
- 已成功定位來源
- 已成功讀取 required rules
- 已成功收斂到 repo 文件

若只能確認其中部分，必須標示：
- partial
- pending confirmation
- 或 not verifiable in this run

---

## 執行步驟

### Step 1. Assumption Inventory
列出目前 `ai/project-context.yaml` 中所有 assumptions / pending confirmations。

### Step 2. Source Availability Check
檢查以下來源是否可用且可解析：
- bootstrap
- governance baseline
- workflow baseline
- shared baseline
- build/test/run/lint/gate 入口

### Step 3. Evidence Check Per Item
對每一條 assumption / pending confirmation，回答：
- 原始內容是什麼
- 原先為何被標成 assumption / pending confirmation
- 目前可使用哪些 evidence
- evidence 是否足以消除
- 若足夠，應改成什麼狀態
- 若不足，缺什麼 evidence

### Step 4. Burn-down Result
輸出：
- 已消除的 assumptions
- 仍保留的 assumptions
- 新增發現的 pending confirmations
- 哪些屬於 governance baseline 缺口
- 哪些屬於 workflow baseline 缺口
- 哪些屬於 shared baseline 缺口
- 哪些屬於 repo runtime / gate / stack 缺口

### Step 5. Minimal File Updates
若 evidence 足夠，可最小必要更新：
- `ai/project-context.yaml`
- 必要時更新 `AGENTS.md`
- 必要時更新 `.github/copilot-instructions.md`

不得因 burn-down 任務重寫整套治理文件。

---

## 輸出格式

### A. Assumption Inventory
### B. Source Availability Check
### C. Evidence Check Per Item
### D. Burn-down Result
### E. File Update Plan
### F. Applied Minimal Changes
### G. Remaining Assumptions / Pending Confirmations

---

## Completion Rules

你不得宣稱：
- 已完全消除所有 assumptions
- 已完整驗證三層 baseline 全部可用
- 已完整確認 repo 所有治理上下文

除非有足夠 evidence 支持。

若存在以下情況，必須明確揭露：
- bootstrap 僅部分可解析
- governance baseline 僅部分可讀
- workflow baseline 僅部分可讀
- shared baseline 僅部分可讀
- repo gate 入口無法完全驗證
- 某些 assumptions 僅能保留，無法在本次 run 消除