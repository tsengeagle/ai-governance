請延續本 repository 已完成的 init 治理結構繼續工作。

這不是 init 任務。  
你現在是在 **既有治理結構之內** 持續工作。

你的目標是：
- 先讀取並遵守既有治理文件
- 必要時參照 bootstrap 與 shared rules baseline，理解本 repo 的治理來源
- 在既有分工下新增或更新內容
- 不得另立一套新的治理邏輯
- 不得把 continue 任務做成 re-init
- 不得為了符合規則而主動強制重構既有實作

---

## 0. 開始前必讀來源

在 continue 任務中，`ai/project-context.yaml` 是 repository 內的第一優先 project authority context；bootstrap 僅用於補充理解原始治理來源，不應取代已收斂的 project context。

在開始前，先讀取並遵守：

### A. 專案 repo 內既有治理文件
1. `.github/copilot-instructions.md`
2. `AGENTS.md`
3. `ai/project-context.yaml`
4. 既有的 `.github/instructions/*.instructions.md`

### B. bootstrap 與 shared rules baseline [若存在]
若存在 `ai/project-context-bootstrap.yaml`，且其中：
- `ai_governance.use_shared_rules: true`
- 有 `governance_repo`
- 有 `shared_rules.required`

則在本次任務需要判斷治理來源、文件落點或規則方向時，應將其作為補充 authority source 參照，用來理解：
- 本 repo 原始治理啟動假設
- governance repo 的 authority source
- 哪些 shared rules 屬於 baseline

注意：
在 continue 任務中，你**不是重新初始化** repo，  
但你必須知道這個 repo 原本是建立在哪個 governance baseline 之上。

若 bootstrap 有 shared rules baseline，但在目前任務中無法完整確認其對應內容，必須標示為：
- `assumption`
- 或 `pending confirmation`

不得假裝 baseline 不存在。

---

## 1. 本次產物必須遵守以下原則

- 除非本次任務明確要求治理對齊，否則 continue 任務不負責系統性盤點 shared rules baseline 缺口，也不負責判定 governance repo 是否已有新規則升級；這類工作屬於 rule alignment 任務範疇
- 不可另立一套新的治理邏輯
- 不可與既有文件大量重複
- 必須尊重既有文件分工：
  - 全域硬規則 -> `.github/copilot-instructions.md`
  - repo operating guide -> `AGENTS.md`
  - stack/path-specific rules -> `.github/instructions/*.instructions.md`
  - project authority context -> `ai/project-context.yaml`
- 若 bootstrap 指定了 shared rules baseline，新的內容不得與 baseline 精神明顯衝突
- 對既有 legacy code 採 grandfathering 原則
- 不可為了讓程式碼更符合規則而主動強制重構既有實作
- 若理想架構與向下相容衝突，優先 backward compatibility
- 涉及 repo-specific 事實時，不得憑印象推測；對於 build/test 指令、type、enum、config、endpoint、schema、分層責任與模組用途，必須先以 source、既有治理文件或實際程式碼為依據確認，無法確認時標示為 `assumption` 或 `pending confirmation`

---

## 2. 開始前先說明以下內容

在真正輸出內容前，請先說明：

1. 這次要新增或更新的產物屬於哪一類
2. 它應該放在哪裡
3. 它與既有文件的邊界
4. 為何不應寫進其他既有文件
5. 本次是否需要參照 bootstrap / governance baseline 才能正確判斷落點或規則方向
6. 本次使用了哪些 authority source
7. 哪些地方仍為 assumption / pending confirmation

---

## 3. 本次 continue 任務的處理方式

### Step 1. Current Governance Context
先整理目前 repo 內已有的治理結構：
- 哪些文件已存在
- 各自角色是什麼
- 本次任務最相關的是哪些文件

### Step 2. Baseline Awareness
若 bootstrap 與 shared rules baseline 可取得，請簡要說明：
- 此 repo 原始治理 baseline 來自哪個 governance repo
- 本次任務與 baseline 有沒有直接關聯
- 是否出現 baseline 相關 pending confirmation

### Step 3. Placement Decision
先決定：
- 這次內容應放在哪個文件
- 為什麼是那個文件
- 為什麼不是其他文件

### Step 4. Minimal Update
執行最小必要修改：
- 優先 patch 既有文件
- 優先延續既有分工
- 避免複製既有規則
- 避免做成 re-init 或整套重建

### Step 5. Result Summary
最後輸出：
1. 本次新增 / 更新了哪些內容
2. 更新了哪些文件
3. 為何這樣放置
4. 哪些內容是沿用 baseline
5. 哪些內容尚待確認
6. 哪些風險或未覆蓋範圍仍存在

---

## 4. Completion Rules

你不得宣稱：
- 已重新建立整套治理結構
- 已完整吸收所有 governance baseline
- 已完全確認所有 repo-specific 事實
- 已完全消除所有不一致

除非有足夠 evidence 支持。

若存在以下情況，必須明確揭露：
- bootstrap baseline 僅部分可確認
- 某些 shared rules 僅能間接推知
- 本次僅更新既有文件，未重新盤點整個 repo
- 某些判斷仍需人工確認

---

## 5. 輸出格式

### A. Current Governance Context
### B. Baseline Awareness
### C. Placement Decision
### D. Output Content
### E. Result Summary
### F. Assumptions / Pending Confirmation / Uncovered Scope