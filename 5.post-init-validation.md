請對這個 repository 的 init 結果執行一次初始化後驗證。

這不是再次 init，也不是 rule alignment。  
你的任務是驗證：

- init 產出的治理文件是否存在
- 文件角色與分工是否正確
- authority sources 是否被正確辨識
- governance / workflow / shared 三層 baseline 是否被正確收斂
- assumptions / pending confirmations 是否被正確揭露
- 是否存在把 baseline 原文直接搬進 repo 的問題
- 是否存在文件角色混淆、重複、或遺漏

不得直接重新初始化。  
不得把驗證任務變成大規模重寫。  
若發現問題，先列差異，再提出最小修正建議。

---

## Authority Sources

### A. init 產物
必須檢查至少以下文件：
- `.github/copilot-instructions.md`
- `AGENTS.md`
- `ai/project-context.yaml`
- `.github/instructions/*.instructions.md`

### B. bootstrap【若存在】
- `ai/project-context-bootstrap.yaml`

必須檢查：
- governance repo
- governance_rules
- workflow_rules
- shared_rules

### C. baseline instance【若本次提供】
若本次任務提供 baseline instance，則它是驗證 init 是否正確收斂的正式 baseline 來源。  
需分開辨識：
- governance baseline
- workflow baseline
- shared baseline

### D. repository 實際結構
- repo 目錄
- representative files
- build/test/run/lint/gate 入口
- stack 跡象

---

## 驗證目標

### 1. File Existence
確認 init 要求的共通文件與 path-specific instructions 是否存在。

### 2. Role Separation
確認以下分工是否清楚：
- `.github/copilot-instructions.md` -> repository-wide hard rules
- `AGENTS.md` -> repo operating guide
- `ai/project-context.yaml` -> authoritative project context
- `.github/instructions/*.instructions.md` -> stack / path-specific rules

### 3. Authority Source Handling
確認 init 是否正確處理：
- bootstrap
- governance baseline
- workflow baseline
- shared baseline

### 4. Convergence Quality
確認治理文件是否為 repo-specific 收斂結果，而不是 baseline 原文照搬。

### 5. Assumption Handling
確認資訊不足處是否被正確標示為：
- assumption
- pending confirmation

### 6. Gate Discovery
確認 build/test/run/lint/static analysis/other gates 是否有被辨識並寫入適當文件。  
若未能確認，是否有被正確揭露。

---

## 執行步驟

### Step 1. Init Output Inventory
列出 init 產出的治理文件與其角色。

### Step 2. Authority Source Summary
列出本次驗證所使用的 authority sources：
- bootstrap 是否存在
- baseline instance 是否存在
- governance / workflow / shared 哪些層已提供
- 哪些層未提供

### Step 3. Verification Checklist
逐項檢查：
- 文件是否存在
- 文件角色是否正確
- 分工是否重疊
- 是否遺漏必要內容
- 是否有 baseline 原文照搬
- 是否有 assumptions 漏揭露
- 是否有 stack/path-specific instructions 缺失
- 是否有 gate discovery 缺失

### Step 4. Validation Findings
將結果分類為：
- passed
- partially passed
- failed
- pending confirmation
- not verifiable in this run

### Step 5. Minimal Correction Plan
若發現問題，先提出：
- 應更新哪些文件
- 為何更新
- 為何是最小必要修改
- 哪些問題不應在此次驗證任務直接修改

---

## 輸出格式

### A. Init Output Inventory
### B. Authority Source Summary
### C. Verification Checklist Result
### D. Validation Findings
### E. Minimal Correction Plan
### F. Optional Minimal Fixes【僅在任務明確要求時】

---

## Completion Rules

你不得宣稱：
- init 已完全正確
- 三層 baseline 已完整吸收
- repo context 已完整確認
- 所有 assumptions 都已妥善消除

除非有足夠 evidence 支持。

若存在以下情況，必須明確揭露：
- governance baseline 未提供或僅部分提供
- workflow baseline 未提供或僅部分提供
- shared baseline 未提供或僅部分提供
- init 產物僅部分符合 baseline
- 某些 path-specific instructions 尚未產出或未充分收斂
- 某些 gate 入口仍待確認