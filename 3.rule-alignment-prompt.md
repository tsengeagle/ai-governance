請針對這個已經完成 init、並已運作一段時間的 repository，執行一次 **AI 治理規則對齊**。

這不是 init 任務，也不是一般 continue 任務。  
你的任務是：

- 讀取目前專案 repo 內已存在的 AI 治理文件
- 讀取 bootstrap 中指定的 governance repo 與 shared rules baseline
- 讀取指定 governance repo 中最新或指定版本的治理規則
- 比對現況與 baseline / 新規則之間的差異
- 以最小必要修改，將本 repo 的治理文件對齊到新的治理規則
- 不得破壞既有文件分工
- 不得把對齊任務變成整套重建
- 不得因規則升級而強制重構既有程式碼

---

## 0. Authority Sources

本次 rule alignment 必須同時以以下來源作為 authority sources：

### A. 專案 repo 內既有治理文件
- `.github/copilot-instructions.md`
- `AGENTS.md`
- `ai/project-context.yaml`
- 既有的 `.github/instructions/*.instructions.md`

### B. bootstrap [若存在]
若存在 `ai/project-context-bootstrap.yaml`，則必須讀取並整理其中：
- `ai_governance.use_shared_rules`
- `ai_governance.governance_repo`
- `ai_governance.shared_rules.base_path`
- `ai_governance.shared_rules.required`

bootstrap 在此任務中的角色是：
- 提供此 repo 原始治理 baseline
- 提供 governance repo authority source
- 提供 shared rules baseline 名單

### C. governance repo
再讀取 bootstrap 或 project context 指定的 governance repo 內容，並依：
- `governance_repo.url`
- `governance_repo.default_ref`
- 若使用者另行指定 branch / tag / commit / release，則以使用者指定為準
- `shared_rules.base_path`
- `shared_rules.required`

載入此次 alignment 所需的 shared rules baseline 與新規則來源。

若 governance repo、shared rules baseline 或對齊版本未明確提供，你不得自行推測新規則內容，必須先回報缺少 authority source。

與 init 任務不同，rule alignment 任務若缺少明確 governance authority source，原則上不得進行實質對齊，只能先回報缺口與待確認事項。

---

## 1. 任務原則

### 1. 先盤點，再修改
先整理：
- 現有治理結構
- bootstrap baseline
- governance repo 新規則
- 三者之間的差異

不可直接重寫文件。

### 2. 這是 alignment，不是 re-init
不得重新初始化整個 repo。  
不得忽略既有文件。  
不得把現有內容全部砍掉重做。

### 3. 最小必要變更
只修改真正需要對齊的地方。  
優先 patch、補充、收斂重複。  
非必要不得整份重寫。

### 4. 尊重既有文件分工
請依下列原則決定落點：
- 全域硬規則 -> `.github/copilot-instructions.md`
- repo operating guide -> `AGENTS.md`
- project authority context -> `ai/project-context.yaml`
- stack/path-specific rules -> `.github/instructions/*.instructions.md`

### 5. 優先更新既有文件，非必要不新增新文件
若既有文件已可承載新規則，優先更新既有文件。  
只有當既有文件分工明顯不足時，才建議新增新文件。

### 6. 保護 legacy 與 backward compatibility
若新規則與既有 legacy 現況衝突：
- 優先 backward compatibility
- 對既有程式碼採 grandfathering
- 規則主要約束新程式碼、實質修改邏輯與新增邏輯
- 不得要求為了規則對齊而全面重構既有程式碼

### 7. 不得虛構
若無法從 repo、bootstrap 或 governance repo 確認某件事：
- 標記為 `assumption`
- 或標記為 `pending confirmation`
- 不可自行補造

### 8. Baseline 與新規則要分開看
你必須區分：
- 哪些屬於 bootstrap 原本就指定的 shared rules baseline
- 哪些屬於 governance repo 後續新增或強化的規則
- 哪些只是既有 repo 尚未正確吸收 baseline
- 哪些才是真正的新一輪治理升級

不得把「baseline 尚未落實」與「新規則升級」混為一談。

不得把 baseline 補齊本身描述成新一輪治理升級完成；baseline 補齊與新規則升級必須在輸出中分開揭露。

---

## 2. 執行步驟

### Step 1. Current Governance Map
列出目前專案 repo 已存在的治理文件與角色分工。

### Step 2. Baseline Summary
列出此 repo 的 bootstrap baseline，包括：
- governance repo
- default ref
- shared rules base path
- shared rules required 清單
- 哪些 baseline 規則可確認已存在於專案治理文件中
- 哪些 baseline 規則尚未明確落實或仍待確認

### Step 3. Governance Source Summary
列出這次對齊所使用的 governance repo、版本、branch、tag 或 commit。  
再整理本次新增 / 強化 / 調整的規則。

### Step 4. Delta Analysis
對每條 relevant rule，回答：
- 這是 baseline 缺口，還是新規則升級
- 目前 repo 內是否已有對應
- 若已有，是否足夠
- 若不足，應更新哪份文件
- 若衝突，應如何處理
- 是否需要 grandfathering / exception / pending confirmation

### Step 5. Alignment Plan
在真正修改前，先輸出：
- 準備更新哪些文件
- 每份文件的更新目的
- 為何放在該文件，而不是其他文件
- 哪些文件不應修改，以及原因
- 哪些屬於 baseline 補齊
- 哪些屬於新規則升級

### Step 6. Apply Minimal Changes
依計畫更新既有治理文件。  
原則：
- 優先 patch
- 保持原有分工
- 避免重複規則散落多處
- 避免整套重寫
- 避免把 shared rules 原文整份複製進專案 repo

### Step 7. Alignment Result
最後輸出：
1. 本次對齊的規則摘要
2. 更新了哪些文件
3. 每份文件更新了什麼
4. 哪些規則已對齊
5. 哪些是 baseline 補齊
6. 哪些是新增治理規則的升級結果
7. 哪些規則因 legacy / backward compatibility 僅部分落地
8. 哪些項目仍待人工確認

---

## 3. 輸出格式

### A. Current Governance Map
- 文件清單
- 每份文件角色

### B. Baseline Summary
- governance repo
- default ref
- required shared rules
- baseline 落實情況

### C. Governance Source Summary
- 本次實際使用的 governance repo / branch / tag / commit / release
- 本次新規則摘要

### D. Delta Analysis
- baseline 缺口
- 新規則升級
- 衝突
- 重複
- 例外

### E. Alignment Plan
- 要改哪些文件
- 改動理由
- 不改哪些文件與原因

### F. Alignment Result
- 已完成對齊項目
- 更新檔案清單
- 各檔案變更摘要

### G. Exceptions / Grandfathering / Pending Confirmation
- grandfathering 區域
- 保留例外
- 待確認事項

---

## 4. Completion Rules

你不得宣稱：
- 已全面完成治理升級
- 已完全符合所有新規則
- 已完全消除所有歷史差異
- 已完整吸收 bootstrap baseline 與 governance repo 全部規則

除非 repo 中確有足夠 evidence 支持。

若存在以下情況，必須明確揭露：
- 某些 baseline 規則僅部分落實
- 某些新規則僅新增到文件，尚未經長期驗證
- 某些規則僅適用於未來新增程式碼
- 某些 legacy 區域僅採 grandfathering，未立即整改
- 某些 bootstrap / governance 判斷仍需人工確認