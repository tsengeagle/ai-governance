請針對這個已經完成 init、並已運作一段時間的 repository，執行一次 AI 治理規則對齊。

這不是 init 任務，也不是一般 continue 任務。  
你的任務是：

- 讀取目前專案 repo 內已存在的 AI 治理文件
- 讀取 bootstrap 中指定的 governance repo 與 baseline【若 bootstrap 存在】
- 讀取本次任務另外提供的 governance baseline instance
- 比對現況與 baseline 之間的差異
- 以最小必要修改，將本 repo 的治理文件對齊到 baseline
- 不得破壞既有文件分工
- 不得把對齊任務變成整套重建
- 不得因規則對齊而強制重構既有程式碼

---

## Authority Sources

本次 alignment 僅可使用以下 authority sources。

### A. 專案 repo 內既有治理文件
必須先讀取並盤點：

- `.github/copilot-instructions.md`
- `AGENTS.md`
- `ai/project-context.yaml`
- 既有的 `.github/instructions/*.instructions.md`

### B. bootstrap【若存在】
若存在 `ai/project-context-bootstrap.yaml`，必須讀取並整理其中：

- `ai_governance.governance_repo`
- `ai_governance.governance_rules.base_path`
- `ai_governance.governance_rules.required`
- `ai_governance.workflow_rules.base_path`
- `ai_governance.workflow_rules.required`
- `ai_governance.use_shared_rules`
- `ai_governance.shared_rules.base_path`
- `ai_governance.shared_rules.required`

若 bootstrap 內存在與架構判定相關的欄位，亦必須讀取並整理：

- `organization.repo_scope`
- `architecture`
- 若存在則包含 `architecture.frontend`
- 若存在則包含 `architecture.microfrontend`
- 若存在則包含 `ui_governance`

bootstrap 在此次任務中的角色僅為：

- 提供此 repo 原始治理 baseline
- 提供 governance repo authority source
- 提供 governance / workflow / shared baseline 名單線索
- 提供此 repo 初始化時的架構上下文與治理落點線索【若存在】

若 bootstrap 不存在，必須明確標示 `bootstrap not found`，不得自行補造 bootstrap 內容。

### C. baseline instance【本次任務提供】
本次 alignment 的正式 baseline authority source，以本次任務提供的 baseline instance 為準。

baseline instance 應至少可包含：

- governance repo URL
- pinned ref【commit / tag / release / branch】
- governance baseline 清單【若有】
- workflow baseline 清單【若有】
- shared baseline 清單【若有】

你必須分開辨識：

#### C1. governance baseline
核心治理規則，例如：
- workflow invariants
- engineering principles
- evidence policy
- 或其他核心治理文件

#### C2. workflow baseline
標準工作流程文件，例如：
- implement-change
- fix-bug
- review-code
- 或其他 workflow 類文件

#### C3. shared baseline
跨 repo 共用規則，例如：
- architecture principles
- design principles
- code quality baseline
- contract / compatibility rules
- commit protocol
- 或其他 shared engineering rules

若本次 baseline instance 只提供其中一層或部分清單，未提供的層不得自行補造。  
應標示為：

- `not provided for this run`
- 或 `not in scope for this run`

### D. authority precedence
若 authority sources 之間有衝突，優先序如下：

1. repository 實際結構與已確認事實
2. baseline instance【本次任務明確提供】
3. bootstrap
4. repo 內既有 local 文件中未經驗證的歷史描述

若存在衝突，必須明確揭露差異，不得自行掩蓋。

### E. baseline immutability rules
若 baseline instance 提供的是 pinned commit，則本次 alignment 預設只對齊到該 pinned commit。  
除非使用者另外明確指定 branch / tag / commit / release，否則不得自行把其他版本視為新規則來源。

不得把 `blob/main` 視為 immutable baseline。  
不得自行推測 governance repo 上還有其他必須納入的 baseline rules。

---

## 任務原則

### 1. 先盤點，再修改
先整理：

- 現有治理結構
- bootstrap baseline【若存在】
- baseline instance
- 各 baseline 與 repo 現況之間的差異

不可直接重寫文件。

### 2. 這是 alignment，不是 re-init
不得重新初始化整個 repo。  
不得忽略既有文件。  
不得把現有內容全部砍掉重做。

### 3. 最小必要變更
只修改真正需要對齊的地方。  
優先 patch、補充、收斂重複。  
非必要不得整份重寫。

### 4. 尊重既有文件分工
請依下列原則決定落點：

- 全域硬規則 -> `.github/copilot-instructions.md`
- repo operating guide -> `AGENTS.md`
- project authority context -> `ai/project-context.yaml`
- stack/path-specific rules -> `.github/instructions/*.instructions.md`

### 5. 優先更新既有文件，非必要不新增新文件
若既有文件已可承載規則，優先更新既有文件。  
只有當既有文件分工明顯不足時，才建議新增新文件。

### 6. 保護 legacy 與 backward compatibility
若 baseline 與既有 legacy 現況衝突：

- 優先 backward compatibility
- 對既有程式碼採 grandfathering
- 規則主要約束新程式碼、實質修改邏輯與新增邏輯
- 不得要求為了 alignment 而全面重構既有程式碼

### 7. 不得虛構
若無法從 repo、bootstrap 或 baseline instance 確認某件事：

- 標記為 `assumption`
- 或標記為 `pending confirmation`
- 不可自行補造

### 8. baseline 補齊與新升級要分開
本次預設 authority source 是 baseline instance。  
若未提供另一個更高版本的 governance ref，則不得宣稱完成「新規則升級」；只能做：

- baseline 對齊
- baseline 缺口補齊
- repo 現況與 baseline 的差異整理

若本次未提供更新版本的 governance authority source，請將「新規則升級」標示為：

`not in scope for this run`

### 9. Frontend / Microfrontend / UI Governance Alignment Rule
本組織預設為 **single-architecture-per-repo**。  
若 repository evidence 顯示此 repo 為前端 repo，則 alignment 時必須將前端治理視為正式 scope 之一；但不得因此把 alignment 任務擴張成重新初始化。

若此 repo 為前端 repo，對齊時必須額外盤點並比對：

- `architecture.style`
- `architecture.frontend`
- 若適用則比對 `architecture.microfrontend`
- 若適用則比對 `ui_governance`
- 既有 `frontend-ui.instructions.md`
- 若適用則比對 `frontend-microfrontend.instructions.md`
- 既有 framework-specific instructions 與 frontend common instructions 之間是否角色衝突或重複

若 repo 不是前端 repo，不得為了對齊而虛構前端治理內容。  
若 repo 為前端 repo，但某些前端治理 authority source 本次未提供，則只能做：

- 現況盤點
- baseline 缺口標示
- 最小必要 patch
- pending confirmation 揭露

不得把未提供的前端規則自行補造成新 baseline。

---

## 執行步驟

### Step 1. Current Governance Map
列出目前專案 repo 已存在的治理文件與角色分工。

若為前端 repo，需額外標示：

- 哪些檔案屬於 frontend common governance
- 哪些檔案屬於 microfrontend governance
- 哪些檔案屬於 framework-specific frontend instructions
- 是否已有 `frontend-ui.instructions.md`
- 是否已有 `frontend-microfrontend.instructions.md`

### Step 2. Baseline Summary
列出此 repo 的 baseline，包括：

- bootstrap 是否存在
- 若存在，bootstrap 內記載的 governance repo / default ref / baseline groups
- 本次實際使用的 baseline instance
- 哪些 governance baseline 規則已提供
- 哪些 workflow baseline 規則已提供
- 哪些 shared baseline 規則已提供
- 哪些 baseline 規則可確認已存在於專案治理文件中
- 哪些 baseline 規則尚未明確落實或仍待確認

若為前端 repo，另需列出：

- bootstrap 是否已有 frontend / microfrontend / ui_governance 線索
- baseline instance 是否有前端共通治理規則
- baseline instance 是否有微前端治理規則
- 現有 repo 是否已對應落點到正確文件

### Step 3. Governance Source Summary
列出本次實際使用的 governance source：

- repo URL
- pinned ref【若有】
- ref type【commit / tag / release / branch】
- governance rules 清單
- workflow rules 清單
- shared rules 清單

如果 repo 內 bootstrap 指向的 baseline 與本次 baseline instance 不一致，必須明確揭露差異，但不得自行推測哪一方為正確，除非有更高優先 authority source。

若前端治理相關 authority source 只在 bootstrap 出現、但未在 baseline instance 提供，必須明確標示該部分是：

- `bootstrap only`
- `not provided for this run`
- 或 `pending confirmation`

### Step 4. Delta Analysis
對每條 relevant rule，回答：

- 這是 governance / workflow / shared 哪一層的 baseline
- 這是 baseline 缺口，還是 repo 已有對應
- 目前 repo 內是否已有對應
- 若已有，是否足夠
- 若不足，應更新哪份文件
- 若衝突，應如何處理
- 是否需要 grandfathering / exception / pending confirmation

若涉及前端 repo，還必須額外回答：

- 這條規則應落在全域文件、project context，還是 frontend-specific instructions
- 這條規則屬於 frontend common、microfrontend，還是 framework-specific
- 現有文件分工是否已正確
- 是否只是缺 patch，還是存在角色重疊
- 是否需要保留 legacy grandfathering

若未提供更新版 governance ref，則不得將差異描述為「新規則升級」，只能描述為：

- baseline 已落實
- baseline 未落實
- baseline 部分落實
- repo local 規則與 baseline 有差異

### Step 5. Alignment Plan
在真正修改前，先輸出：

- 準備更新哪些文件
- 每份文件的更新目的
- 為何放在該文件，而不是其他文件
- 哪些文件不應修改，以及原因
- 哪些屬於 governance baseline 補齊
- 哪些屬於 workflow baseline 補齊
- 哪些屬於 shared baseline 補齊
- 哪些屬於 frontend common baseline 補齊【若適用】
- 哪些屬於 microfrontend baseline 補齊【若適用】
- 哪些項目因缺少 authority source 而不處理

### Step 6. Apply Minimal Changes
依計畫更新既有治理文件。  
原則：

- 優先 patch
- 保持原有分工
- 避免重複規則散落多處
- 避免整套重寫
- 避免把 baseline 原文整份複製進專案 repo

若為前端 repo，應優先：

- patch 既有 `frontend-ui.instructions.md`
- patch 既有 `frontend-microfrontend.instructions.md`【若存在且適用】
- patch `ai/project-context.yaml` 中與 `architecture.frontend` / `architecture.microfrontend` / `ui_governance` 相關區塊
- 僅在既有分工明顯不足時才建議新增新文件

### Step 7. Alignment Result
最後輸出：

1. 本次對齊的規則摘要
2. 更新了哪些文件
3. 每份文件更新了什麼
4. 哪些 governance 規則已對齊
5. 哪些 workflow 規則已對齊
6. 哪些 shared 規則已對齊
7. 哪些是 baseline 補齊
8. 哪些項目不在此次 scope
9. 哪些規則因 legacy / backward compatibility 僅部分落地
10. 哪些項目仍待人工確認

若為前端 repo，另需說明：

11. 哪些 frontend common 規則已對齊
12. 哪些 microfrontend 規則已對齊【若適用】
13. 哪些 UI governance 項目已對齊
14. 哪些前端治理事項仍待人工確認

---

## 輸出格式

### A. Current Governance Map
- 文件清單
- 每份文件角色

### B. Baseline Summary
- bootstrap 是否存在
- bootstrap baseline【若存在】
- baseline instance
- baseline 落實情況

### C. Governance Source Summary
- 本次實際使用的 governance repo / ref
- 本次使用的 governance rules 清單
- 本次使用的 workflow rules 清單
- 本次使用的 shared rules 清單
- 是否存在更新版 governance authority source
- 若不存在，明確標示：`no newer governance authority source provided`

### D. Delta Analysis
- governance baseline 缺口
- workflow baseline 缺口
- shared baseline 缺口
- baseline 已落實
- baseline 部分落實
- 衝突
- 重複
- 例外

若為前端 repo，另需列出：
- frontend common baseline 缺口
- microfrontend baseline 缺口【若適用】
- UI governance 缺口
- framework-specific 與 frontend common 之間的重疊或衝突

### E. Alignment Plan
- 要改哪些文件
- 改動理由
- 不改哪些文件與原因

### F. Alignment Result
- 已完成對齊項目
- 更新檔案清單
- 各檔案變更摘要

### G. Exceptions / Grandfathering / Pending Confirmation
- grandfathering 區域
- 保留例外
- 待確認事項

---

## Completion Rules

你不得宣稱：

- 已全面完成治理升級
- 已完全符合所有未提供 authority source 的新規則
- 已完整吸收 governance repo 全部未列出的規則
- 已完整消除所有歷史差異

除非 repo 中確有足夠 evidence 支持。

若存在以下情況，必須明確揭露：

- 某些 governance baseline 規則僅部分落實
- 某些 workflow baseline 規則僅部分落實
- 某些 shared baseline 規則僅部分落實
- 某些 frontend common 規則僅部分落實【若適用】
- 某些 microfrontend 規則僅部分落實【若適用】
- 某些 UI governance 規則僅部分落實【若適用】
- 某些規則僅新增到文件，尚未經長期驗證
- 某些規則僅適用於未來新增程式碼
- 某些 legacy 區域僅採 grandfathering，未立即整改
- 某些 bootstrap / governance 判斷仍需人工確認